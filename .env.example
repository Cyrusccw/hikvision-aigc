DEEPSEEK_API_KEY=
DEEPSEEK_MODEL=deepseek-chat

# Keyframe image generation. If omitted, the studio uses a local preview-mode keyframe
# so the workflow remains testable before production credentials are configured.
SEEDREAM_API_KEY=
SEEDREAM_BASE_URL=
SEEDREAM_MODEL=seedream

# Shot video generation (next integration)
SEEDANCE_API_KEY=
SEEDANCE_BASE_URL=
SEEDANCE_MODEL=seedance-2.0

DATABASE_URL=
BLOB_READ_WRITE_TOKEN=
